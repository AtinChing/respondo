# Respondo backend — keep in sync with pyproject.toml (uv is canonical for this repo).
# Install: pip install -r requirements.txt
# LLM: GEMINI_API_KEY or GOOGLE_API_KEY (video analysis + report generation via Railtracks / LiteLLM).

chromadb==1.5.5
fastapi==0.135.2
google-genai==1.69.0
httpx==0.28.1
litellm==1.82.6
opencv-python-headless>=4.10.0.84,<5.0.0
pdfplumber==0.11.9
pydantic==2.12.5
python-dotenv==1.2.2
python-multipart==0.0.22
railtracks==1.3.6
uvicorn[standard]==0.42.0